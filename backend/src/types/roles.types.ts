export interface CreateRoleDTO {
    role_name: string,
    description: string,
    status_id: number,
}
export interface UpdateRoleDTO {
    role_name: string,
    description: string,
    status_id: number,
}